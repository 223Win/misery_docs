import LuauCodeBlock from "../../components/LuauCodeBlock"
import SUNC from '@/components/sUNC';

# Functions

Functions defined by the executor in C++

Misery has quite a few libraries that follow <SUNC/>/UNC conventions, and you can use them in your scripts.